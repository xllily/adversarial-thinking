# Resource read policy

Every resource lookup must prove that the authenticated principal owns the
requested resource before returning its body. A route-registration test does
not prove this object-level authorization rule.
