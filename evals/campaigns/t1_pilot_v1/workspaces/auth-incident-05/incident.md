# Authentication incident

The current model says intermittent login rejection is a network timeout. The
countermodel says stale cache entries are responsible. The team proposes either
increasing retries or purging the cache globally.
